# TRUTH LEDGER

One line per trades.csv column and per emitted insight. A number is trustworthy only
when its row here says **verified** or **fixed** — anything else is unproven.

- **verified** — live code path traced end-to-end with file:line evidence; stamped on the right bar, frozen correctly, population known.
- **fixed** — defect found, fix shipped WITH a structural guard (test/gate/whitelist).
- **voided** — computed on a corrupted-era column; invalid until re-derived from a post-fix baseline run.
- **pending** — not yet audited. Assume nothing.
- **pending-fix** — defect known (spec written), fix NOT yet in code.
- **out-of-scope** — owned by another workstream (sweep rebuild).

Status date: 2026-07-10 (Deep Value Pass Areas A/B/C/D done; canonical baseline run `1feb2db`, 2026-07-09).
DETECTION_FIXES_SPEC.md Fixes 1/2/3 **LANDED**; TRUTH_FIXES_SPEC.md T1/T2/T3
**SHIPPED**; TRUTH_FIXES_SPEC_2.md T4/T5 **SHIPPED**. Guards green
(tests/test_dedupe.py, test_ob_alert_freeze.py, test_aggregate_eligibility.py,
test_truth_ledger.py, test_p2_window_clamp.py, test_structure_signals.py — full
suite 77 passed 2026-07-04).

**2026-07-10 SYSTEM AUDIT — Batch 0 (ledger hygiene) DONE.** The 07-09 canonical
run (`1feb2db`) IS the first post-fix baseline: every T1-T5 / Fix 1-3 / D1-D5 fix
landed before it. So the 6 previously-voided insights are UN-VOIDED to
`verified (population)` — their source columns are populated and varied in the
baseline CSV (sampled all 33,838 rows). "verified (population)" = the corruption
era is over and the feeding columns are real; it does NOT yet re-audit each
insight's own aggregation math (that is Batch 3). The sweep leg stays
out-of-scope (sweep-rebuild workstream) — un-voided only the fvg / freshness /
structure legs. Baseline integrity is now guarded by
tests/test_truth_ledger.py::test_baseline_ex_corrupted_columns_are_real (fails if
the canonical CSV's ex-corrupted columns ever regress to null/constant).

**Stamp bump scope:** this bump records that the void condition is cleared. It
does NOT blanket re-verify the `verified` column rows — those line-refs are
re-confirmed against HEAD only in the batch that audits them (Batch 2 row-build,
Batch 3 insights). Batch 1 (detection columns) is DEFERRED: the break gates were
removed 2026-07-10 AFTER this run, so every detection-derived VALUE here is stale
until the user triggers a fresh canonical run. Plumbing (col X derives from Y) is
still auditable; detection VALUES are not.

**2026-07-10 SYSTEM AUDIT — Batch 3 (insights + Excel/email) DONE.** All
def-anchored Insights line-refs re-mapped to HEAD (6 had drifted a few lines each:
ob_freshness_comparison :527→:533, regime_verification :559→:566, generate_verdict
:605→:612, verify_capturable/is_peak_metric :708→:723/:715, driver buckets/two-way
:815/:870→:814/:869, counterfactual :628→:627, zone register :1126→:1128, Excel
tabs :1403/:1468→:1411/:1475; plus prose refs _trades_csv :1217→:1216, IST-drop
:3237→:3329, write :3458→:3480). Guarded by
tests/test_truth_ledger.py::test_insight_ledger_line_refs_point_at_their_function
(proven to go RED on a stale def-ref). Aggregation math re-audited on the 07-09
baseline: score_validation / confluence_attribution / ob_freshness_comparison /
setup_badge run on real, varied source columns and produce coherent verdicts
(score = no edge, confluences = NOISE — consistent with prior findings, honestly
reported, NOT acted on). ob_freshness now keys on ob_touches (0/1/2 real variance);
alert_seq confirmed dead (all=1). SMC DISCUSSION POINT (flagged, not scored): the
07-09 baseline is gates-ON — the score/confluence "no edge" read is real for the
detection-independent legs but detection-sourced break_* slices stay values-stale
until Batch 1. Sweep leg remains sweep-rebuild-owned.

**2026-07-10 DEEP VALUE PASS — Area A (freeze) + Area C (derivations) DONE.**
Area A: all 8 OB-build frozen-by-design columns proven (see their rows). Area C:
15 CSV-derivable columns RECOMPUTED from their inputs against the full 33,838-row
canonical population, 0 unexplained mismatches. Exact match (0 mismatch): bias,
alert_utc_hour, session, ob_session, fill_session, pd_alignment, event,
reversed_from_extreme, trend_pd_agree, r_capture_ratio, ob_to_fill_hours,
killzone_alignment, headline_exclusion, eligible_for_headline. sl_distance_atr:
28,191 exact + 5,647 reachable-within-the-6dp-atr-round-band, 0 unreachable
(divides by full-precision _h1_atr; CSV stores atr_at_ob at 6dp, so bit-exact
recompute is impossible near a rounding boundary — formula correct, precision-
limited, NOT a bug). Guards: tests/test_truth_ledger.py::
test_area_c_derivations_recompute_on_canonical_csv +
::test_area_c_sl_distance_atr_reachable_via_atr_rounding_band, both bite-proven RED.
NOTE: `session`/`ob_session`/`fill_session` are NY-LOCAL DST-aware (via _ts_hour_ny
→ America/New_York), NOT the "UTC-fixed" the session row's caveat claims — code is
truth; the ledger caveat below is STALE and corrected in the session row.
NOT population-recomputed here (need row-build internals absent from the CSV):
pd_zone/pd_pct (frozen DR object), ob_age_h1_bars/bars_break_to_pullback (df H1
index), leg_retrace_pct_at_alert (impulse_start_price), r_if_exit_tp1/2 (exit-engine
replay) — their FORMULAS are unit-tested on synthetic OBs in
backtest/test_h1_only.py::test_edge_lab_columns (drives live _build_row).

**2026-07-10 DEEP VALUE PASS — Area B (ATR normalisation) + Area D (insight
population) DONE.** Area B: proved all SIX *_atr columns (ob_range_atr,
fvg_size_atr, impulse_leg_atr, sl_distance_atr, sl_wick_depth_atr,
sl_max_adverse_after_sweep_atr; break_* = Batch 1, sweep_* out-of-scope) divide by
the ONE frozen ob['h1_atr'] (== atr_at_ob at 6dp), never a per-scan/hard-coded ATR.
Two live-code guards in backtest/test_h1_only.py: ::
test_area_b_all_atr_cols_share_the_one_h1_atr_denominator (drives simulate_h1_only_dual
to a real SL-sweep exit, re-runs with h1_atr doubled, asserts every *_atr HALVES
and atr_at_ob DOUBLES — bite-proven RED: a divisor swapped to a constant fails to
scale) + ::test_area_b_atr_source_uses_h1_atr (source tripwire on the exact
divisor bindings). Canonical-file anchor: tests/test_truth_ledger.py::
test_area_b_sl_distance_atr_divides_atr_at_ob_on_canonical — the ONLY *_atr whose
numerator is fully in the CSV (|entry-sl_initial|) recomputes as num/atr_at_ob for
all rows (28,191 bit-exact + 5,647 reachable-in-the-6dp-band, 0 unreachable on the
full 33,838; a 1.5× wrong denominator makes ALL 33,838 unreachable — bite proven).
Area D: masks proven to DROP force-closed rows ON REAL DATA (10,434 timeout/
window_end/never_filled rows in the file, 0 survive _eligible_mask) and the headline
reconcile proven ON THE FILE (eligible_for_headline column == live _headline_exclusion
rule == _eligible_mask, 0 mismatch/33,838; sum(pnl_usd where eligible) = −271,467.0 =
headline; eligible+excluded partitions all_pnl cleanly; eligible ⊆ real_filled).
Peak-vs-fill gate re-asserted (is_peak_metric flags mfe/touch cols; verify_capturable
REJECTS a 47%-touched/23%-capturable claim). Guards extend
tests/test_aggregate_eligibility.py: ::test_area_d_masks_drop_force_closed_rows_on_canonical_data
+ ::test_area_d_headline_reconciles_on_canonical_file +
::test_area_d_peak_metric_gate_rejects_a_touch_claim, all bite-proven RED (mask
disabled → force-closed leak in; gate forced verified=True → touch claim passes).

## Layer map (where numbers are born)

1. **Detection** — smc_radar.py / smc_detector.py / dealing_range.py (shared with live).
2. **Replay** — backtest/replay_engine.py walks bars, holds OB state, yields alerts.
3. **Simulation + row build** — backtest/h1_only_simulator.py; `_build_row` at :1274, row dict at :1465-1639.
4. **Insights** — backtest/insights.py (stats) + backtest/h1_only_reporting.py (sections, Excel tabs, trades.csv writer `_trades_csv` at :1216).
5. **Email/report** — backtest/reporting_email.py + backtest/render_report.py.

## Population caveats

- **RESOLVED 2026-07-03:** Fix 1 (150-bar live-window clamp) and Fix 2 (ATR-scaled dedupe) are in code. All pre-fix runs remain non-comparable; the first post-fix full run is the new baseline.

## trades.csv columns

Row build: h1_only_simulator.py `_build_row` at :1274; row dict at :1465-1639. "src" = the line the value is read/written in the row dict. Line-refs re-mapped 2026-07-10 (Batch 2) and guarded by tests/test_truth_ledger.py::test_row_build_ledger_line_refs_point_at_the_column.

| column | src (h1_only_simulator.py) | stamped when | status | note |
|---|---|---|---|---|
| pair | :1485 | alert | verified | yield payload scalar (replay_engine.py:648) |
| alert_ts | :1486 | alert | verified | = h1_ts of the closed-bar scan (replay_engine.py:649); alert>bos guard at replay_engine.py:529-554 |
| alert_bar_ts | :1487 | alert | verified | yield payload scalar (replay_engine.py:687) — just-closed bar's ts |
| alert_seq | :1491 | alert | verified | yield payload scalar from fire_count (replay_engine.py:654); traded row is always first fire per zone (dedupe run_backtest.py:161-169) |
| bos_timestamp | h1_only_simulator.py `_build_row` (`ob.get("bos_timestamp")`) | OB build (immutable, both paths) | fixed | FREEZE (Deep Value A, 2026-07-10): FROZEN in the canonical CSV (replay merges by exact ob_timestamp, refreshes only fvg — guards test_ob_alert_freeze.py bite-proven). **LIVE LEAK FOUND + FIXED 2026-07-10:** zone.py refresh used to RE-STAMP bos_timestamp (:271) on a proximity-fallback match (find_matching_slate_zone branch b, smc_radar.py:3328 — same price, different break), so a refreshed live zone carried the LATEST break's ts, not formation's. Owner confirmed leak, not intended. Fix: refresh now keeps the formation value (back-fill-if-None only), bringing live to backtest parity. Guard test_zone_roundtrip.py::test_ob_identity_fields_all_frozen_on_refresh + ::test_ob_identity_fields_backfill_once_when_legacy_unset (bite-proven RED). RESIDUAL: 6 live zones on disk may still carry a pre-fix re-stamped value; they self-heal on next re-formation (fix stops future drift, does not rewrite existing). 2026-07-02 detection audit 3e classification; alert>bos guard replay_engine.py:529-554. 2026-07-09 EVENT-CANDLE FIX: derives from the TRUE break candle, not the confirmation candle. **2026-07-10 GATES REMOVED**: with the distance buffer now 0, the true-break candle == the fire candle on every continuous break (differs only across a weekend gap), so bos_timestamp = the bar the close first crossed the level. bos_idx=_idx_from_ts(candle_ts) (smc_radar.py:866) inherits it. See event_candle_delta row |
| fill_ts | :1493 | fill | verified | 2026-07-02 truth-chain audit |
| exit_ts | :1494 | exit | verified | 2026-07-02 truth-chain audit |
| direction | :1495 | OB build (immutable) | verified | detection audit 3e: immutable event fact. FREEZE (Deep Value A, 2026-07-10): truly frozen BOTH paths — backtest replay merges by exact ob_timestamp and refreshes only fvg; live zone.py refresh never touches it. Guards: tests/test_ob_alert_freeze.py::test_ob_build_fields_read_directly_no_snapshot + ::test_replay_merge_only_refreshes_fvg_not_identity_fields + test_zone_roundtrip.py::test_ob_identity_fields_freeze_split_on_refresh, all bite-proven RED |
| bias | :1496 | derived from direction | verified | Area C recompute-verified (0 mismatch, 33,838 rows); LONG if bullish else SHORT |
| model | :1497 | constant | verified | literal "h1_only" |
| event | :1498 | OB build | verified | Area C recompute-verified (0 mismatch, 33,838 rows); deterministic label from immutable bos_tag/bos_tier |
| entry_zone | :1499 | alert | verified | proximal only (run_backtest.py:345); truth-chain audit (fills) |
| entry | :1500 | alert | verified | truth-chain audit (fills) |
| sl_raw | :1504 | alert | verified | 2026-07-02 truth-chain audit |
| sl_initial | :1505 | alert | verified | 2026-07-02 truth-chain audit |
| tp1 / tp2 | :1506-1507 | alert (levels on 200-bar window) | **fixed** | T5: compute_phase2_levels now fed the last LIVE_P2_H1_BARS=200 closed bars via _closed_bars_at_alert (h1_only_simulator.py:364, used at :519), matching live P2's fetch depth; was unbounded history (TP selection drifted with run start). Guard tests/test_p2_window_clamp.py PASS |
| tp1_rr / tp2_rr | :1508-1509 | alert (levels on 200-bar window) | **fixed** | T5: same 200-bar clamp as tp1/tp2 (RR derived from the clamped-window TP pick) |
| tp1_wick | :1513 | alert | verified | 2026-07-15 TP-placement change. tp1 above is the ZONE-EDGE (traded) level; tp1_wick is the raw swing-high/low wick it replaced. Source: compute_phase2_levels out["tp1_wick"] (smc_detector.py:2124), placed by _tp_zone_edge (smc_detector.py:1861). None only when no TP1. |
| tp1_wick_rr | :1514 | alert | verified | 2026-07-15. RR of the raw wick (`abs(wick-entry)/risk`), so wick-vs-zone RR gap is a clean two-column diff. Source: compute_phase2_levels out["tp1_wick_rr"] (smc_detector.py:2125). |
| tp1_zone_source | :1515 | alert | verified | 2026-07-15. "zone" = opposing-OB near edge used; "wick" = fallback (no qualifying opposing candle in walk-back, OR h4_wall TP which has no candle). Source: compute_phase2_levels out["tp1_zone_source"] (smc_detector.py:2126) / _tp_zone_edge return (smc_detector.py:1861). |
| tp2_wick | :1516 | alert | verified | 2026-07-15. Raw swing wick behind the zone-edge tp2. None when no TP2. Source: compute_phase2_levels out["tp2_wick"] (smc_detector.py:2139). |
| tp2_zone_source | :1517 | alert | verified | 2026-07-15. Same "zone"/"wick" meaning as tp1_zone_source, for TP2. None when no TP2. Source: compute_phase2_levels out["tp2_zone_source"] (smc_detector.py:2140). |
| exit_price | :1518 | exit | verified | 2026-07-02 truth-chain audit |
| exit_reason | :1519 | exit | verified | 2026-07-02 truth-chain audit |
| r_realised | :1520 | exit | verified | 2026-07-02 truth-chain audit; P&L source of truth |
| r_if_exit_tp1 / r_if_exit_tp2 | :1521-1522 | exit | verified | 2026-07-02 truth-chain audit |
| pnl_usd | :1523 | exit | verified | r_realised × risk_usd |
| mfe_r / mae_r | :1524-1525 | exit | verified | 2026-07-02 truth-chain audit (excursions) |
| sl_bar_was_sweep | :1536 | SL exit | verified | 2026-07-02 truth-chain audit |
| sl_swept_then_tp1 | :1537 | SL exit | verified | 2026-07-02 truth-chain audit; hint only, peak-metric gate |
| sl_wick_depth_atr | h1_only_simulator.py (calc beside sl_bar_was_sweep) | SL exit | verified | 2026-07-08; max(0, stop-candle overshoot beyond fired stop)/ob['h1_atr']; ≥0, None on non-SL / no ATR; population = filled SL exits; logging-only, NO gate; guard test_h1_only.py::test_sl_wick_depth_atr. Area B (2026-07-10): divisor is _h1_atr_sl = ob.get("h1_atr") (h1_only_simulator.py:1002, used :1008) — SAME frozen denominator; proven by the live scaling guard test_area_b_all_atr_cols_share_the_one_h1_atr_denominator (drives a real SL-sweep exit, halves when h1_atr doubles) + source tripwire test_area_b_atr_source_uses_h1_atr, both bite-proven |
| sl_max_adverse_after_sweep_atr | h1_only_simulator.py (calc beside sl_wick_depth_atr) | OUTCOME-time (SL sweep) | verified | 2026-07-08; max(0, further run beyond fired stop over SL_SWEEP_LOOKBACK_BARS after stop bar)/ob['h1_atr']; ≥0; None on non-SL / non-sweep / no ATR; EXIT-TRACK ONLY (leakage as entry feature); C6 hint; guard test_h1_only.py::test_edge_lab_columns. Area B (2026-07-10): divisor _h1_atr_sl = ob.get("h1_atr") (:1002, used :1044) — SAME frozen denominator; proven by the live scaling + source guards (both bite-proven) |
| bars_sl_to_tp1_touch | h1_only_simulator.py (calc beside sl_wick_depth_atr) | OUTCOME-time (SL exit) | verified | 2026-07-08; 1-indexed H1 bars from stop bar to first TP1 touch in-direction within lookback; None if never touched; EXIT-TRACK ONLY; C6 touch-hint not a fill; guard test_h1_only.py::test_edge_lab_columns |
| sl_recovered_to_entry | h1_only_simulator.py (calc beside sl_wick_depth_atr) | OUTCOME-time (SL sweep) | verified | 2026-07-08; bool — after a sweep, did price trade back to entry (BE) within lookback; None on non-SL / non-sweep; EXIT-TRACK ONLY; scratched-not-won middle case; guard test_h1_only.py::test_edge_lab_columns |
| sl_distance_atr | h1_only_simulator.py (calc in _build_row beside atr_at_ob) | fill | verified | 2026-07-08; abs(entry - sl_initial)/ob['h1_atr']; risk width in ATR; uses sl_initial (traded stop) NOT sl_raw; point-in-time clean; None if no ATR; ENCODED (replaces ex-pasted corrupted CSV col); guard test_h1_only.py::test_edge_lab_columns. Area C 2026-07-10: population reachability-verified — 28,191/33,838 bit-exact, 5,647 reachable within the 6dp-atr round-band, 0 unreachable (divides full-precision _h1_atr while atr_at_ob stores 6dp; precision-limited recompute, formula correct). Guard test_truth_ledger.py::test_area_c_sl_distance_atr_reachable_via_atr_rounding_band (bite-proven). Area B (2026-07-10): the ANCHOR *_atr — its numerator |entry-sl_initial| is fully in the CSV, so it directly proves the divisor is atr_at_ob: test_truth_ledger.py::test_area_b_sl_distance_atr_divides_atr_at_ob_on_canonical recomputes num/atr_at_ob for the full population (all reachable) and a 1.5× wrong denominator makes every row unreachable (bite-proven). Also in the live scaling guard (halves when h1_atr doubles) |
| r_capture_ratio | h1_only_simulator.py (calc in _build_row beside atr_at_ob) | OUTCOME-time (exit) | verified | 2026-07-08; r_realised/mfe_r; fraction of best favorable move kept; None when mfe_r<=0 (no 0/0); EXIT-TRACK ONLY (uses r_realised); ENCODED (replaces ex-pasted corrupted CSV col); guard test_h1_only.py::test_edge_lab_columns. Area C recompute-verified (0 mismatch, 33,838 rows) |
| trend_pd_agree | h1_only_simulator.py (calc in _build_row beside atr_at_ob) | alert | verified | 2026-07-08; bool — trade WITH h1_trend AND pd_alignment=='aligned'; both confluences same way; None if either missing; point-in-time clean; ENCODED (replaces ex-pasted corrupted CSV col); guard test_h1_only.py::test_edge_lab_columns. Area C recompute-verified (0 mismatch, 33,838 rows) |
| be_arm_bar_touched_entry | :1552 | BE arm | verified | 2026-07-02 truth-chain audit; be_eps FP-boundary tolerance at :670 (2026-07-03 fix, G10 rule b) |
| ob_to_fill_hours | :1554 (calc :1068-1075) | fill | verified | Area C recompute-verified (0 mismatch, 33,838 rows); wall-clock hours (fill_ts − ob_timestamp), round 2; None if never filled |
| bars_break_to_pullback | :1556 (calc :1081-1090) | fill | verified | bar count bos_ts→fill on df index; deterministic from immutable inputs |
| bars_to_exit / bars_to_tp1 / bars_to_tp2 | :1557-1559 | exit | verified | 2026-07-02 truth-chain audit |
| ob_age_h1_bars | :1560 (calc :345) | alert | verified | bar count on df index ob_ts→alert_ts; deterministic, weekend-gap-safe |
| ob_timestamp | h1_only_simulator.py `_build_row` (`ob.get("ob_timestamp")`) | OB build (immutable) | verified | FREEZE (Deep Value A, 2026-07-10): truly frozen BOTH paths — it is the identity key the backtest merge and live find_matching_slate_zone both match ON, so it can never be re-stamped. Guards test_ob_alert_freeze.py + test_zone_roundtrip.py::test_ob_identity_fields_freeze_split_on_refresh (bite-proven). detection audit 3e: immutable event fact. 2026-07-09 EVENT-CANDLE FIX: OB walkback (smc_radar.py:929) now starts from the corrected true-break candle, so ob_timestamp moves for the ~15% of events whose OB candle changed (34% of CHoCHs). "verified" always meant the freezing/plumbing, not "right candle" — the candle is now right. LIVE dedupe caveat: find_matching_slate_zone (smc_radar.py:3305) keys on ob_timestamp — see event_candle_delta note |
| event_candle_delta | h1_only_simulator.py `_build_row` (`ob.get("event_candle_delta")`); produced dealing_range.py `_push_event` `event_candle_delta` field; wired smc_radar.py active_obs.append | OB build (immutable event fact, frozen at formation) | verified | int bars = confirm_idx − true_break_idx. **2026-07-10 GATES REMOVED**: the distance (bos_disp/choch_disp) AND body gates are gone — a break fires on a bare close through the level, so bos_disp=choch_disp=0. Consequence: delta is 0 on EVERY continuous break (fire candle == true-break candle), and >0 ONLY across a session/weekend gap where `_traded_through` delays the fire past the true weekend-open break (walk-back re-anchors to it). None for CHoCH_FAILED (not a break). Population = all BOS/CHoCH-derived zones. Logging-only, NO gate. Guard: tests/test_event_candle_fix.py (rewritten for the gates-removed invariant) |
| pd_zone | :1567 (calc :220) | row build, entry vs frozen DR | verified | 0.5 split, deterministic; dealing_range frozen-by-design (live same, smc_radar.py:1042-1052) |
| pd_alignment | :1568 (calc :241) | row build | verified | Area C recompute-verified (0 mismatch, 33,838 rows) from bias + pd_zone. Its 0-mismatch is ALSO indirect proof pd_zone is self-consistent (pd_alignment is a pure fn of pd_zone) |
| pd_pct | :1569 (calc :1218) | row build, entry vs frozen DR | verified | deterministic; None when DR invalid |
| reversal_pct | :1573 (src :1073) | structure event (frozen) | verified | detection audit 3e: immutable; epoch-drift PARKED per spec (near-zero after Fix 1) |
| reversed_from_extreme | :1574 (calc :1257) | row build, derived | verified | Area C recompute-verified (0 mismatch, 33,838 rows) from bos_tag + reversal_pct; 0.0 ambiguity documented at :1077-1081 |
| score | :1575 (calc :367-385, slice :382) | simulate time, on alert-time ob_view (payload) | **fixed** | T1+T4 view feeds run_scorecard alert-time bos_verdict + payload touches_at_alert/fvg_at_alert; T5 clamps the scoring slice to LIVE_P2_H1_BARS=200 via _closed_bars_at_alert (:382). Guards test_ob_alert_freeze.py + test_p2_window_clamp.py PASS |
| structure_pts | :1576 | simulate time, on alert-time ob_view | **fixed** | smc_detector.py:2110 now reads the alert-time verdict via ob_view (T1) |
| sweep_pts | :1577 | alert | out-of-scope | sweep rebuild workstream |
| fvg_pts | :1578 | simulate time, on alert-time ob_view (payload) | **fixed** | Fix 3c/3d + T4: fvg_at_alert now travels in the alert PAYLOAD (not the re-stamped dict), routed into scoring via ob_view. Guard test_ob_alert_freeze.py T4 cases PASS |
| freshness_pts | :1579 | simulate time, on alert-time ob_view (payload) | **fixed** | Fix 3a/3d + T4: touches_at_alert now travels in the alert PAYLOAD, routed into scoring via ob_view. Guard test_ob_alert_freeze.py T4 cases PASS |
| killzone_pts | :1580 | simulate time | verified | scored on immutable ob_timestamp via shared DST-aware ts_in_killzone (smc_detector.py:1937); max 1 |
| confluences_present | :1581 (calc :261) | simulate time | **fixed** | derived from breakdown, now computed on alert-time ob_view (T1 + Fix 3) |
| session | :1582 (calc :130) | alert | verified | Area C recompute-verified (0 mismatch, 33,838 rows). CORRECTED 2026-07-10: session buckets are NY-LOCAL DST-aware (_fill_session→_ts_hour_ny→America/New_York, h1_only_simulator.py:109-136 + :95-106), NOT UTC-fixed — the old "UTC-fixed, NOT DST-aware" caveat was STALE (code is truth). `session` = session at ALERT time (fed alert_ts twice at :1555). No DST asymmetry vs killzone; both DST-aware |
| weekend_blocked | :1587 (calc :157) | fill | verified | deterministic IST calc from fill_ts + config; False for non-crypto |
| sl_collision | :1588 (stamp :674, :829) | exit | verified | SL-first collision rule; truth-chain audit (exits) |
| bos_tag / bos_tier | :1589-1590 | OB build (immutable, both paths) | fixed | FREEZE (Deep Value A, 2026-07-10): bos_tag frozen BOTH paths (never touched by refresh). bos_tier FROZEN in canonical CSV (replay refreshes only fvg). **bos_tier LIVE LEAK FOUND + FIXED 2026-07-10:** zone.py refresh used to RE-STAMP bos_tier (:268) on a proximity-fallback match — same leak class as bos_timestamp; owner confirmed not intended. Fix: refresh no longer overwrites it (bos_tier always set at from_fresh, no back-fill needed). Guards test_ob_alert_freeze.py::test_ob_build_fields_stamped_once_in_radar + ::test_replay_merge_only_refreshes_fvg_not_identity_fields + test_zone_roundtrip.py::test_ob_identity_fields_all_frozen_on_refresh (all bite-proven RED). detection audit 3e: immutable event facts (now holds both paths) |
| bos_verdict | :1596 | alert (payload scalar via ob_view) | **fixed** | T1 SHIPPED 2026-07-03; payload scalar at replay_engine.py:673. FREEZE guarded by tests/test_ob_alert_freeze.py::test_bos_verdict_from_payload — now drives the LIVE build_alert_ob_view (h1_only_simulator.py:1179), bite-proven RED 2026-07-10 (Deep Value Area A) |
| bos_sequence_count | :1602 | OB build (immutable) | verified | detection audit 3e: immutable event fact |
| break_tier / break_close_atr / break_excess / break_body_atr | :1603-1606 | one-time re-grade when window complete | **fixed** | 2026-07-10 NOTE: `break_dist_atr` named in SYSTEM_AUDIT_HANDOFF is NOT a separate column — it == break_close_atr (owner-confirmed). No new column; CANONICAL.md's "→109" bump is void. compute_break_quality returns exactly {tier, close_beyond_atr, body_atr, excess} (smc_detector.py:2861-2864); no distance field exists or is needed. | Fix 3b LANDED (_bq_regraded in replay_engine.py:373-381); verify distribution shift at baseline review. **2026-07-10 GATES REMOVED**: break_body_atr (dealing_range `_break_body_atr`) is now a PURE OBSERVATION — the body gate that used it is gone; every close-through break is logged regardless of body. The value is the raw signal a future DATA-DERIVED body/distance gate would read (BOS_BODY_ATR_MULT / STRUCTURE_CHOCH_*_ATR_MULT retained only as grading references / re-arm placeholders, gate nothing). compute_break_quality still normalises against those floors for the score |
| ob_range_atr | :1608 (calc :1275) | row build from frozen high/low | verified | deterministic from immutable high/low + frozen h1_atr. Area B (2026-07-10): divides by the ONE ob['h1_atr'] via _atr_norm (h1_only_simulator.py:1360-1361) — proven by the live scaling guard test_h1_only.py::test_area_b_all_atr_cols_share_the_one_h1_atr_denominator (halves when h1_atr doubles) + source tripwire ::test_area_b_atr_source_uses_h1_atr, both bite-proven |
| fvg_size_atr | :1609 (calc :1287) | row build from refreshed fvg | **fixed** | Fix 3c LANDED (fvg refresh on re-surface, replay_engine.py:424); alert-time snapshot guard PASS. Area B (2026-07-10): same ONE ob['h1_atr'] denominator via _atr_norm — proven by test_area_b_all_atr_cols_share_the_one_h1_atr_denominator + source tripwire (bite-proven) |
| impulse_leg_atr | :1610 (calc :1299) | row build from frozen prices | verified | deterministic from immutable impulse_start_price + bos_swing_price. Area B (2026-07-10): same ONE ob['h1_atr'] denominator via _atr_norm — proven by test_area_b_all_atr_cols_share_the_one_h1_atr_denominator + source tripwire (bite-proven) |
| atr_at_ob | :1611 (calc :1305) | OB build (formation ATR, frozen both paths) | fixed | FREEZE (Deep Value A, 2026-07-10): reads ob['h1_atr'] (formation ATR), NOT the per-scan alert['h1_atr'] — that distinction IS the freeze, asserted by test_ob_alert_freeze.py::test_ob_build_fields_read_directly_no_snapshot (bite-proven). FROZEN in canonical CSV (replay merge refreshes only fvg). **LIVE LEAK FOUND + FIXED 2026-07-10:** zone.py refresh used to RE-STAMP h1_atr (:261) on a proximity-fallback match, so a refreshed live zone's atr_at_ob became the refresh-scan ATR, silently warping every LIVE *_atr metric that reads it (distance-in-ATR smc_radar.py:2338, zone-width-in-ATR :2551). Owner confirmed leak. Fix: refresh keeps the formation ATR (back-fill only when h1_atr==0.0). Live *_atr metrics now measure a frozen zone in its formation volatility (stable rank, matches the metric's definition). Guard test_zone_roundtrip.py::test_ob_identity_fields_all_frozen_on_refresh + ::test_ob_identity_fields_backfill_once_when_legacy_unset (bite-proven). RESIDUAL: 6 live zones may carry a pre-fix value; self-heal on re-formation. Was "frozen BY DESIGN" — now true both paths |
| ob_body_ratio | h1_only_simulator.py:1619 (emit) / :1371 (read), stamped smc_radar.py:1204; LIVE persist zone.py `body_ratio` (_FIELD_ORDER:68, from_fresh:178, frozen in refresh) | OB build (frozen by design) | fixed | chosen OB candle's own body/range, walk-back loop smc_radar.py:880-901; range>0 guarded (is_valid_ob_candle already rejects range==0); observe-only per DECISION_GUARDRAILS.md A3, no gate. FIX 2026-07-06: zone.py `_FIELD_ORDER`+`from_fresh` did NOT carry it → dropped to null on active_obs.json (backtest read raw OB dict, was fine; LIVE was blind). Now persisted + frozen on refresh (never re-stamped) + back-filled once for legacy zones. Guard: test_zone_roundtrip.py test_walkback_fields_persist_and_freeze. None only for zones written before this fix, until they re-fire. |
| ob_walkback_depth | h1_only_simulator.py:1620 (emit) / :1372 (read), stamped smc_radar.py:1211; LIVE persist zone.py `walkback_depth` (_FIELD_ORDER:68, from_fresh:179, frozen in refresh) | OB build (frozen by design) | fixed | oversized_count + undersized_count + doji_count at acceptance (smc_radar.py:880-901); 0 = first candidate passed; sums ALL three skip reasons so knob-sweep (MIN_OB_RANGE_ATR_MULT>0) is counted; observe-only per DECISION_GUARDRAILS.md A3, no gate. FIX 2026-07-06: same zone.py drop bug as ob_body_ratio → was null on active_obs.json live. Now persisted + frozen + back-filled once. Guard: test_zone_roundtrip.py test_walkback_fields_persist_and_freeze. |
| chop_at_alert | h1_only_simulator.py:1623 (emit) / :1373-1379 (compute) via smc_detector.compute_choppiness_index; LIVE compute+emit Phase2_Alert_Engine.py `trade_data["chop_at_alert"]` (~:2823), email line ~:1528. NOT frozen on the zone (alert-anchored, recomputed at the alert bar). | Alert bar (computed at alert time, NOT OB formation) | verified | Daily Choppiness Index: CHOP = 100 × log10(Σ TR / (dayHigh − dayLow)) / log10(N), over the H1 bars of the alert's SERVER trading day up to & incl. the alert (last CLOSED) bar. TR = H−L on the first bar of the day, else max(H−L, \|H−prevClose\|, \|L−prevClose\|). Result 0–100 rounded 3dp: LOW (≤~38)=trending, HIGH (≥~62)=ranging. **Day boundary = the MT5 server day [prev 21:00, 21:00) UTC** — the SAME window PDH/PDL use (pool_builder._server_date, SERVER_UTC_OFFSET_HOURS=3, no DST); both feeds arrive UTC so live (Twelve Data) and backtest (MT5 parquet) resolve the identical day — this identity is mandatory (CHOP must describe the same day as the D1 levels, never a rolling window / naive midnight / resample). **Anchored at the alert bar, NOT OB formation** (reads current regime, not stale OB-birth context) — CHOP-only; no other column re-anchored. Look-ahead clean: backtest passes the last bar strictly BEFORE alert_ts (candle A) as the anchor so the still-forming candle B is excluded; live anchors on drop_forming(df_h1)'s last closed bar. **None-conditions**: when the alert day has <CHOP_MIN_BARS (=5) bars, fall back to the previous fully-closed server day; None only when BOTH that day and the fallback are unmeasurable (denominator 0 / <2 bars). Never fabricates 0.0. **Observe-only — gates/scores/filters NOTHING.** The "choppy day = worse trades" idea is an UNPROVEN hypothesis this column exists to test; asserted nowhere. Population: every alert (0% fire at bar ≤5, so the prev-day fallback is rare in practice). Smart-sample validation (CHF cache 2024-06→2025-06, 366 days): CHOP inversely tracks net/spread (trending days net/spread 0.79–0.99 → CHOP 26–29; choppy days 0.08–0.20 → CHOP 67–70); dist min26/median47/max70. **CAVEAT (research-time, not a bug):** endpoint stability is N-dependent — late-day anchors (N≥18, 78% of alerts) wobble ≤6 CHOP pts over ±2 bars, but early-day (N 6-11, 22% of alerts) wobble ~8pts median; a future slice should treat early-day CHOP as fuzzy (tertiles, not a sharp 38/62 cut). Math/boundary/look-ahead guards: test_h1_only.py::test_choppiness_index_math / ::test_choppiness_index_prev_day_fallback / ::test_choppiness_day_boundary_matches_server_date / ::test_choppiness_anchor_includes_its_bar_no_lookahead; freeze-absence guard: test_ob_alert_freeze.py (computed-not-frozen + no ER read). |
| fvg_present | :1624 | alert (fvg_at_alert, payload) | **fixed** | Fix 3c/3d + T4: stamped at the yield as a PAYLOAD scalar (replay_engine.py:680, survives re-fire re-stamping of the shared dict); ob_view overwrites both fvg + fvg_at_alert keys. FREEZE guarded by test_ob_alert_freeze.py::test_fvg_frozen_at_alert / test_touches_fvg_from_payload_not_restamped_dict — now drive the LIVE read_frozen_ob_fields + build_alert_ob_view (h1_only_simulator.py:1179), bite-proven RED 2026-07-10 (Deep Value Area A) |
| fvg_state | :1627 (helper) | row build (payload fvg via ob_view) | **fixed** | Fix 3d + T4: helper classifies against the payload fvg_at_alert carried on `ob_at_alert` (returned by the LIVE read_frozen_ob_fields, h1_only_simulator.py:1179). Guard test_ob_alert_freeze.py bite-proven RED 2026-07-10 (Deep Value Area A) |
| fvg_mitigation | :1631 | alert (fvg_at_alert, payload) | **fixed** | Fix 3c/3d + T4: payload scalar via ob_view. FREEZE guarded by test_ob_alert_freeze.py (LIVE read_frozen_ob_fields, bite-proven RED 2026-07-10, Deep Value Area A) |
| ob_touches | :1635 | alert (touches_at_alert, payload) | **fixed** | Fix 3a/3d + T4: stamped at the yield as a PAYLOAD scalar (replay_engine.py:679, survives re-fire re-stamping); ob_view overwrites both touches + touches_at_alert keys. FREEZE guarded by test_ob_alert_freeze.py::test_touches_frozen_at_alert / test_zero_touches_at_alert_honoured — now drive the LIVE read_frozen_ob_fields (h1_only_simulator.py:1179), bite-proven RED 2026-07-10 (Deep Value Area A). Meaning: touches between structural event and ALERT |
| sweep_present | :1636 | frozen snapshot | out-of-scope | sweep rebuild workstream |
| ob_session | :1643 (calc :123) | OB build ts | verified | Area C recompute-verified (0 mismatch, 33,838 rows). NY-LOCAL DST-aware (from immutable ob_timestamp via _ts_hour_ny); the old "UTC-fixed caveat as session" was STALE — corrected same as session row |
| fill_session | :1644 (calc :130) | fill | verified | Area C recompute-verified (0 mismatch, 33,838 rows — incl. the never_filled→alert-hour fallback). NY-LOCAL DST-aware (not UTC-fixed). CAVEAT: never_filled rows fall back to ALERT hour (:133-135) — population mixes fill-time and alert-time labels; never_filled rows are audit-only |
| killzone_alignment (col) | :1617 (calc :198) | derived | verified | Area C recompute-verified (0 mismatch, 33,838 rows) from ob_in_killzone + fill_in_killzone + fill_ts; deterministic 4-way bucket + never_filled |
| ob_in_killzone | :1645 (calc :190, :175-176) | OB build ts | verified | DST-aware via shared live engine smc_detector.ts_in_killzone (:137) |
| fill_in_killzone | :1646 (calc :194) | fill | verified | same shared DST-aware engine |
| killzone_alignment | :1647 (calc :198) | derived | verified | deterministic 4-way bucket + never_filled |
| h1_trend | :1648 | alert | verified | yield payload scalar (replay_engine.py:656) — immune to post-alert OB mutation |
| trend_alignment | :1649 | alert | **fixed** | 2026-07-05 parity fix: now derived at fire via the SHARED helper smc_detector.derive_trend_alignment (replay_engine.py:607) — SAME implementation live Phase 2 uses (Phase2_Alert_Engine.py:2492), fed the flip-pending state so a with-trend read is demoted to counter_trend/ambiguous while a CHoCH flip is pending. Vocabulary unified on live's values (with_trend/counter_trend/ambiguous); the old backtest dialect (against_trend/no_trend) is GONE. Passed as payload scalar (:660). Guards: tests/test_structure_signals.py (branch table + live-parity + single-implementation source-assert). Pre-fix runs' values non-comparable |
| structure_ranging_at_alert | :1654 (payload) | alert (payload scalar) | verified | S2: `walls["structure_v2"]["ranging"]` snapshotted at the replay yield (replay_engine.py:667, `_sv2.get("ranging")` at :592); every row, None only if structure_v2 missing (degraded walls). Ranging counter now flip-reset (S1). Guard test_structure_signals.py + freeze case |
| flip_pending_at_alert | :1655 (payload) | alert (payload scalar) | verified | S2: `structure_v2["flip_unconfirmed"]` at yield (replay_engine.py:668, read :593); a CHoCH armed but not yet Confirmation-BOS. Every row, None if structure_v2 missing. Guard test_structure_signals.py + freeze case |
| flip_pending_dir_at_alert | :1656 (payload) | alert (payload scalar) | verified | S2: `structure_v2["choch_pending_dir"]` mapped up→bullish/down→bearish (replay_engine.py:669, map :590 read :594); None when no pending flip OR structure_v2 missing. Guard test_structure_signals.py |
| leg_extreme_at_alert | :1662 (payload) | alert (payload scalar) | verified | S3 SUPPORT (not screened): a-priori leg extreme (max High / min Low) over closed slice bars ts>=ob_timestamp, computed in replay fire block (has h1_slice) + carried as payload scalar (replay_engine.py:667). None if ob_timestamp missing or slice empty. Guard test_structure_signals.py + freeze case |
| leg_retrace_pct_at_alert | :1663 (calc in _build_row) | alert-derived (payload extreme + placed entry) | verified | S3: (leg_extreme−entry)/(leg_extreme−impulse_start)×100 for bullish, mirrored bearish; from leg_extreme_at_alert (payload) + entry (placed limit) + frozen impulse_start_price. None when extreme unstamped / impulse_start missing / denominator≤0. >100 VALID, not clamped. Guard test_structure_signals.py math cases + freeze case — now drives the LIVE leg_retrace_pct helper (h1_only_simulator.py), bite-proven RED under pytest 2026-07-10 (Deep Value A) |
| leg_extreme_clipped | :1664 (payload) | alert (payload scalar) | verified | S3 SUPPORT (not screened): True when ob_timestamp predates the point-in-time slice start (extreme may be understated); keeps leg_retrace honest (replay_engine.py:668). None on measurement failure. Guard test_structure_signals.py |
| dr_ceiling_broken_at_ob | :1668 (read from frozen dr) | OB build (frozen snapshot) | verified | S4: read off frozen ob["dealing_range"]["ceiling_broken"], sourced get_dealing_range valid branch (smc_detector.py) ← compute_pd_position ceiling_is_placeholder ← h4_range ceiling_broken (h4_range.py:233). None when snapshot invalid/legacy. Frozen at OB build (no re-stamp). Guard test_structure_signals.py snapshot case — now drives the LIVE read_s4_broken_flags helper, bite-proven RED under pytest 2026-07-10 (Deep Value A) |
| dr_floor_broken_at_ob | :1669 (read from frozen dr) | OB build (frozen snapshot) | verified | S4: read off frozen ob["dealing_range"]["floor_broken"], same chain as dr_ceiling_broken_at_ob (h4_range floor_broken, h4_range.py:234). None when snapshot invalid/legacy. Guard test_structure_signals.py snapshot case — via LIVE read_s4_broken_flags (bite-proven under pytest 2026-07-10, Deep Value A) |
| setup_badge / setup_badge_kind | :1672-1673 (calc via ob_view) | row build, live classifier on alert-time view | **fixed** | classify_setup (smc_detector.py:2462, reads touches/fvg :2479-2484) now receives alert-time touches/fvg via ob_view (T1); sweep input stays sweep-workstream-owned. 2026-07-05: its trend_alignment input (:2510 keys off `== 'with_trend'`) now comes from the shared derive_trend_alignment, so the badge no longer diverges from live when a CHoCH flip is pending. Guard: tests/test_structure_signals.py trend-alignment parity. Pre-fix runs' values non-comparable |
| day_state_at_fill | dict-spread from _pool_features_at_fill (end of h1_only_simulator.py) → pool_builder.features_at_alert (generic asof helper) / day_state | row build, from H1 bars strictly BEFORE fill_ts (FILL-anchored 2026-07-16: latest info the trade holds; alert-time status can be stale by fill; observation-only so no look-ahead wall). never_filled rows (fill_ts=None) → all-None dict | pending | PD/PW pools (DAILY_BIAS_V4_SPEC §1.2): INSIDE / EXPANSION_UP / EXPANSION_DOWN / SWEPT_HIGH / SWEPT_LOW / BOTH_SIDES from the two PD pool statuses. Day = MT5 server date (21:00 UTC boundary, import_mt5.py). None when no completed prior day in frame or pool layer errored. Guards: tests/test_pool_builder.py (no-look-ahead + determinism + full status machine). NOT yet in any canonical run |
| pdh_status_at_fill / pdl_status_at_fill / pwh_status_at_fill / pwl_status_at_fill | pool_builder.pool_status via features_at_alert (generic asof helper, same spread) | row build, closed H1 bars from pool birth to fill_ts (FILL-anchored 2026-07-16; never_filled → None) | pending | intact / swept / broken per DAILY_BIAS_V4_SPEC §1.1. broken = close beyond + next-close hold (N=1); swept = wick-through-close-back OR failed break (approved 2026-07-13); broken overrides an earlier swept; unconfirmed close-beyond reports prior status. **broken is NOT terminal (2026-07-16, rule A): a broken level that later closes back across the origin side is reclaimed → downgraded to swept (label reflects NOW); a reclaim can itself re-break. Level never moves, only the status label.** Same machine feeds EQ clusters (eq_pools.py imports pool_status). Levels = prev completed server day / week, resampled from the SAME H1 frame (proven vs MT5's own D1/W1 files: D1 99.98–100% exact over 4,800 days × 9 pairs; W1 mismatches are MT5 aggregate extremes absent from the H1 series itself — unreachable by the walker). None on thin history. Guard tests/test_pool_builder.py |
| dist_next_pool_above_atr / dist_next_pool_below_atr / next_pool_above_tier / next_pool_below_tier | pool_builder.trade_features via features_at_alert (generic asof helper, same spread; fill_ts) | row build, at fill (fill-anchored 2026-07-16; never_filled → None) | pending | distance from ENTRY to nearest still-INTACT pool each side, in ob['h1_atr'] units (same frozen-formation-ATR denominator as the other *_atr columns); tier PD/PW, weekly outranks daily on a tie. None when no intact pool that side / thin history / no ATR. Guard tests/test_pool_builder.py |
| trade_toward_pool | pool_builder.trade_features via features_at_alert (generic asof helper, same spread; fill_ts) | row build, at fill (fill-anchored 2026-07-16; never_filled → None) | pending | True when the trade direction points at the nearest intact pool overall (DAILY_BIAS_V4_SPEC §1.3 hypothesis 2). None when no intact pool either side or direction missing. Guard tests/test_pool_builder.py |
| last_sweep_age_h1 / last_sweep_tier | pool_builder.trade_features via features_at_alert (generic asof helper, same spread; fill_ts) | row build, at fill (fill-anchored 2026-07-16; never_filled → None) | pending | closed H1 bars since the most recent sweep event across the four pools, and its tier (PD/PW). None when nothing swept yet this day/week. Guard tests/test_pool_builder.py |
| eqh_above_dist_atr / eqh_above_size / eql_below_dist_atr / eql_below_size | eq_pools.features_from_clusters via _eq_features_at_alert (dict-spread at end of h1_only_simulator.py row dict, after the pool spread) | row build, from H1 bars strictly BEFORE alert_ts (immutable price history — no yield freeze needed; pool-columns precedent) | pending | EQH/EQL clusters (2026-07-14): nearest INTACT equal-highs shelf above entry / equal-lows shelf below entry — distance in ob['h1_atr'] units (SAME frozen formation-ATR denominator as every other *_atr column) + member count (2 = pair, 3+ = stronger shelf). Cluster = ≥2 confirmed raw-geometry lookback-3 swings (dealing_range.detect_swings, min_leg_atr_mult=None — raw pool used ONLY here, owner-approved) within EQ_TOL_ATR=0.20×ATR of the running extreme (0.20 not 0.10: MT5-vs-TD feed gap p50 ~1 pip ≈ 0.10×ATR would flip membership between live and backtest feeds; grid 0.10/0.20/0.30 later), consecutive members ≤120 bars apart, lookback 240 bars. Level = member extreme (where stops sit). Lifecycle = pool_builder.pool_status (ONE status machine with PD/PW pools). None when layer can't run; counts are real 0 when it ran and found none. Observe-only, NO gate. Guards: tests/test_eq_pools.py (no-look-ahead + determinism + formation + lifecycle + column contract). NOT yet in any canonical run |
| eq_trade_toward | eq_pools.features_from_clusters (same spread) | row build, at fill (fill-anchored 2026-07-16; never_filled → None) | pending | True when trade direction points at the nearest intact EQ shelf overall (mirror of trade_toward_pool). None when no intact shelf either side / direction missing. Guard tests/test_eq_pools.py |
| eq_sl_gap_atr / eq_sl_at_risk | eq_pools.features_from_clusters (same spread; sl = sl_initial, the traded stop) | row build, at fill (fill-anchored 2026-07-16; never_filled → None) | pending | THE instant-death hypothesis columns: signed distance from the traded stop to the nearest intact same-side EQ shelf beyond the entry→stop path, in ob['h1_atr'] units. POSITIVE = shelf sits beyond our stop → a stop-run to the shelf takes us out first (we ARE the liquidity); NEGATIVE = stop tucked behind the shelf. at_risk = 0 < gap ≤ 1.0 ATR (stop inside one bar's stop-run reach of the magnet). bullish: (sl−EQL)/atr; bearish: (EQH−sl)/atr. Guard tests/test_eq_pools.py::test_sl_at_risk_* (both directions, sign + band) |
| eq_last_sweep_age_h1 / eq_last_sweep_side | eq_pools.features_from_clusters (same spread) | row build, closed bars before alert | pending | closed H1 bars since the most recent EQ-shelf sweep across ALL clusters (spent shelves included — the sweep EVENT is the signal) + which side ('high'/'low'). None when nothing swept in the 240-bar lookback. Guard tests/test_eq_pools.py::test_wick_through_close_back_marks_swept |
| eq_intact_above_count / eq_intact_below_count | eq_pools.features_from_clusters (same spread) | row build, at fill (fill-anchored 2026-07-16; never_filled → None) | pending | how many intact EQ shelves sit above/below entry (context density). Real 0 when detection ran and found none; None only when the layer couldn't run. Guard tests/test_eq_pools.py |
| weekly_pd_position_at_alert | weekly_pd.build_features via _weekly_pd_features_at_alert (dict-spread at end of h1_only_simulator.py row dict, after the eq spread) | row build, from H1 bars strictly BEFORE alert_ts (immutable price history — no yield freeze needed; pool/eq-columns precedent) | pending | Weekly PD zone (2026-07-15, owner-approved observe-only): (entry − PWL) / (PWH − PWL) where PWH/PWL = last COMPLETED week's high/low, read from the SAME pool-layer resample (pool_builder.levels_at → server_days → server_weeks of our own H1, proven vs MT5 W1). Boundaries are FIXED — NOT re-anchored when price closes beyond them (owner decision: keep sight of last week's actual high/low). So the value is intentionally allowed to run **> 1.0** (price closed above last week's high — broken up) or **< 0.0** (below last week's low — broken down); the raw % IS the break signal, no clamp. Daily PD deliberately skipped (redundant w/ H4; daily H/L already the PD pools). None when < 1 completed prior week in frame / degenerate range / layer errored. Guards: tests/test_weekly_pd.py (no-look-ahead + determinism + fixed-boundary break-through + agreement + column contract). NOT yet in any canonical run |
| weekly_range_high_at_alert / weekly_range_low_at_alert | weekly_pd.build_features (same spread) | row build, at alert | pending | The last COMPLETED week's high (PWH) and low (PWL) that anchor weekly_pd_position — the raw boundary prices, logged so the owner can re-derive/override the % manually. Same PWH/PWL the PD/PW pool columns use (one weekly derivation, never a second). None on thin history. Guard tests/test_weekly_pd.py |
| weekly_pd_zone_at_alert | weekly_pd.build_features → _zone (same spread) | row build, at alert | pending | "premium" / "discount" from weekly_pd_position, split at exactly 0.5 (owner anchor, no dead-band): >= 0.5 = premium (expensive), < 0.5 = discount (cheap). A broken-up weekly (>1.0) still resolves as premium, broken-down (<0.0) as discount. None when the position is None. Guard tests/test_weekly_pd.py |
| pd_zone_agreement_at_alert | weekly_pd.build_features → pd_zone_agreement (same spread) | row build, at alert | pending | H4-vs-weekly premium/discount agreement: both_premium / both_discount / mixed. H4 read = the frozen dealing-range pd_pct/100 computed at row build (h1_only_simulator.py `_pd_position_01`, 0-1); weekly read = weekly_pd_position. Each side split at 0.5. THE hypothesis column: when big picture + local both say expensive/cheap, a continuation trade buys/sells into a reversion zone. None when either PD read is missing. Observe-only, NO gate. Guard tests/test_weekly_pd.py::test_agreement_* |
| approach_speed_atr_at_fill | approach_quality.features_at_fill via _approach_features_at_fill (dict-spread, LAST in h1_only_simulator.py row dict, after the weekly-PD spread) | row build, closed H1 bars strictly BEFORE the fill bar | pending | RETRACE_QUALITY_SPEC (2026-07-15): directed net close move toward the zone over the 4 closed bars before fill (3 close-to-close steps) / ob['h1_atr'] (SAME frozen formation-ATR denominator as every other *_atr column). Positive = moving toward the zone; negative = drifting away (V-spike tell). FILL-time, NOT alert-time — banned from the §5.1 alert-time screen (look-ahead wall); legal action class = order_rule only. None when never_filled / <7 closed bars before fill / direction invalid / atr invalid. Live twin = approach_*_at_email on the phase2_zones entry (scan-time, re-stamped each email BY DESIGN — rolling observation, not frozen OB state). Guard tests/test_approach_quality.py. NOT yet in any canonical run |
| approach_body_ratio_at_fill | approach_quality.features_at_fill (same spread) | row build, closed H1 bars strictly BEFORE the fill bar | pending | mean |C−O|/(H−L) over the same 4 pre-fill bars, zero-range bars skipped; None when never_filled / <7 bars / all 4 zero-range. ATR/direction NOT needed. 3dp. FILL-time class, same look-ahead ban. Guard tests/test_approach_quality.py |
| approach_er_at_fill | approach_quality.features_at_fill (same spread) | row build, closed H1 bars strictly BEFORE the fill bar | pending | Kaufman ER over the 7 pre-fill closed bars (6 steps): |C[b7]−C[b1]|/Σ|ΔC|; None when never_filled / <7 bars / zero denominator (a real 0.0 is never faked — efficiency_ratio_at_alert convention). 3dp. FILL-time class, same look-ahead ban. Guard tests/test_approach_quality.py |
| news_blocked / news_event_title / news_event_currency / news_event_source / news_event_ts | run_backtest.py:205-209 | post-walk, from frozen alert_ts | verified (stamping) | class-safe (deterministic fn of frozen alert_ts). INFORMATIONAL — does NOT gate headline (h1_only_reporting.py:142-176). **Event `ts_utc` was +4h wrong pre-2026-07-06 (D4): FF feed time is UTC, was mis-stamped as US Eastern — news_filter.py:152-158 now stamps UTC directly, guard test_ff_xml_time_is_utc. Pre-fix news_blocked is on wrong rows.** |
| ist_blocked | run_backtest.py:210 (calc :193) | post-walk, from frozen alert_ts | verified (stamping) | HARD gate — rows dropped from everything except audit (:201, h1_only_reporting.py _headline_exclusion :142-176) |
| killzone_blocked | run_backtest.py:211 (calc :177) | post-walk, from frozen alert_ts | verified (stamping) | informational; does not gate |
| killzone_windows | run_backtest.py:212 | config label | verified | static label |
| setup_id | h1_only_reporting.py:3323-3333 | report build, pre-CSV | verified | atomic counter (lock file, :33); same ID in CSV/Excel/HTML/email |
| eligible_for_headline / headline_exclusion | h1_only_reporting.py:1230-1231 | CSV write, from THE one rule _headline_exclusion (:142-176) | verified | Area C recompute-verified (0 mismatch, 33,838 rows) from exit_reason + ist_blocked + weekend_blocked. self-describing headline membership; sum(pnl_usd where eligible) == headline, enforced by reconcile assert :3446-3457. Area D (2026-07-10): reconcile PROVEN ON THE FILE — the column == live _headline_exclusion == _eligible_mask (0 mismatch/33,838); sum(pnl_usd where eligible)=−271,467.0=headline; eligible+excluded partitions all_pnl; eligible ⊆ real_filled. Guard tests/test_aggregate_eligibility.py::test_area_d_headline_reconciles_on_canonical_file (bite-proven) |
| alert_utc_hour | run_backtest.py:213 | from frozen alert_ts | verified | Area C recompute-verified (0 mismatch, 33,838 rows); int(alert_ts.hour), deterministic |

## Defects found by THIS audit (not in DETECTION_FIXES_SPEC.md)

### D1 — bos_verdict logs the LAST fire's verdict, not the traded alert's (2026-07-02)

- Evidence chain:
  - run_backtest.py:144-159 — the replay generator is FULLY drained into `pair_alerts` before any simulation.
  - run_backtest.py:162+ — rows are built AFTER the whole walk, but the yield passed `ob` by reference (replay_engine.py:652).
  - replay_engine.py:567 — `ob["bos_verdict"]` is re-assigned on EVERY fire of the same zone.
  - run_backtest.py:161-169 — only the FIRST fire per zone is traded; its row (h1_only_simulator.py:1336) reads whatever the LAST fire stamped.
- Impact: multi-fire zones can log a verdict from hours/days after the traded alert (holding↔fading flip). Single-fire zones unaffected.
- Class: identical to spec Fix 3d's class — mutable OB state read at row-build time. Correct fix: carry `bos_verdict` in the yield payload (like h1_trend) or stamp `bos_verdict_at_alert`; guard = extend the 3e regression test to mutate `bos_verdict` post-alert and assert the row keeps the alert-time value.
- **FIXED 2026-07-03 (T1):** verdict carried as yield-payload scalar (replay_engine.py fire block) + alert-time ob_view in simulate_h1_only_dual feeds scoring/badge/row. Guard: tests/test_ob_alert_freeze.py T1 cases incl. source tripwire — PASS. Pre-fix bos_verdict-keyed slices remain suspect for multi-fire zones until the baseline rerun.

### D2 — stale comment claims news gates aggregates — FIXED 2026-07-03

- Was: h1_only_reporting.py claimed news_blocked rows were excluded from aggregates. Code truth: news never gates (the one rule is _headline_exclusion :142-176). Comment corrected to point at the one rule (now at :1260-1261 and :3414).

### D3 — cross-run aggregator ignores headline eligibility (2026-07-03)

- Evidence chain (pre-fix line refs; the raw-load bug is what T2 closed):
  - aggregate_runs.py:73 — `_load_trades` reads trades.csv raw, no eligibility filter.
  - trades.csv contains timeout/window_end rows with force-closed r_realised (audit-only per the settled unresolved-trade policy) and ships `eligible_for_headline` exactly so consumers can filter (h1_only_reporting.py:1230-1231) — pre-fix aggregate_runs never read it.
- Impact: EVERY cross-run insight (compute_overall, instrument_verdicts, score_validation, pair_session_matrix, group_comparison, freshness, regime, generate_verdict) included arbitrary-price force-closed rows. Per-run email/Excel numbers were clean; the cross-run layer over-counted.
- **FIXED 2026-07-03 (T2):** `_eligible_mask` + hard assert in aggregate_runs.py; entry_zone fill-rate keeps never_filled only. Guard: tests/test_aggregate_eligibility.py (3 cases incl. string-bool round-trip + legacy CSV) — PASS. All prior combined/VERDICT outputs are stale until re-aggregated.

### D4 — news event ts_utc was shifted +4h (feed time treated as US Eastern, is actually UTC) (2026-07-06)

- Evidence chain:
  - news_filter.py:145-147 — FairEconomy weekly XML `<time>` parsed naive (`"%m-%d-%Y %I:%M%p"`).
  - news_filter.py:155-156 (pre-fix) — stamped `tzinfo=America/New_York` then `.astimezone(UTC)`, i.e. ADDED the ET offset (+4h EDT / +5h EST). Fallback :161 shared the same wrong assumption.
  - The feed `<time>` is already UTC — proven against 3 independent release anchors in state/ff_calendar_cache/ff_calendar_2026-06-29.xml: ISM `2:00pm` = 14:00 UTC = 10:00 ET ✓; Spanish Flash CPI `7:00am` = 07:00 UTC = 09:00 CEST ✓; Japan data `11:30pm` = 23:30 UTC = 08:30 JST ✓.
- Impact: EVERY event's `ts_utc` was 4h late (summer). Two blast zones: (1) live Phase 2 email — a real ISM Services PMI at 19:30 IST (7:30pm) rendered as 23:30 IST (11:30pm); (2) `is_news_blackout()` + the backtest news filter gated the ±window 4h late, so `news_blocked` marked the wrong rows. news_blocked is INFORMATIONAL (never gates the headline, per D2), so P&L is unaffected — but any news-attribution slice pre-fix is on wrong rows.
- Class: naive-parse-then-wrong-tz. Correct fix: stamp `local.replace(tzinfo=UTC)` directly — no shift.
- **FIXED 2026-07-06:** news_filter.py:152-158 stamps UTC directly (Eastern conversion + buggy fallback deleted). Guard: backtest/test_news_filter.py `test_ff_xml_time_is_utc` — asserts ISM `2:00pm` → 14:00 UTC → 19:30 IST and Spanish CPI `7:00am` → 07:00 UTC — PASS. Any pre-fix news_blocked-keyed slice is on wrong rows until re-run.

### D5 — Phase-1 sweep/FVG/context timestamps read integer row numbers (reset_index poisoning) (2026-07-06)

- Evidence chain:
  - smc_radar.py:453 — Phase-1 H1 frame built with `tailed.reset_index()` (NO drop=True): moves the UTC DatetimeIndex into a `Datetime` COLUMN and leaves a plain integer RangeIndex.
  - smc_detector.py (pre-fix) read `df.index[i]` expecting a timestamp in THREE spots — sweep stamp (:1338), swept-swing stamp (:1344), FVG `_idx_to_iso` (:1483) — each returned the ROW NUMBER (e.g. 122), poisoning the ISO fields.
  - Same integer-index trap silently broke TWO context-tag helpers: `_prior_trading_day_hl` (:1033) and `_session_hl_until` (:1079) iterated `df.index` → every `.date()` threw → caught → returned (None, None), so prior-day/session tags NEVER fired for Phase 1.
- Impact: LIVE Phase-2 trade email only. Sweep/FVG overlays were stamped `"122"/"105"/"129"` (row numbers); Phase 2's `locate_ob_candle_idx` fails `datetime.fromisoformat("122")` → overlays dropped or landed on the wrong candle (the NZDUSD email the owner flagged). Prior-day/session context badges silently absent. NO effect on P&L, scoring, or the backtest (backtest builds its own DatetimeIndex-real df).
- Class: reset_index-drops-datetime-into-column; index read where a timestamp was assumed.
- **FIXED 2026-07-06:** two shared resolvers in smc_detector.py — `_iso_for_idx` / `_ts_for_idx` (read `Datetime`/`Date` column first, index fallback) — wired into all three stamps; `_row_timestamps` used by both context-tag loops. Mirrors smc_radar `_ts_for_idx`. Guard: tests/test_sweep_timestamp_resolution.py (drives observe_phase1_sweep on a reset-index df; asserts no ISO field is a bare row number — proven to go RED when the bug is reintroduced). Full suite 47 passed.

## Insights (backtest/insights.py + reporting sections)

Two consumers, two populations — this matters:
- **Per-run (Excel tabs / email)**: population = `trades_all` (IST+weekend hard-dropped by `_is_hard_blocked` :191, applied `trades_all = [...] ` :3329) filtered to real fills. Matches headline. CLEAN.
- **Cross-run (aggregate_runs.py)**: FIXED by T2 — `_eligible_mask` (:48) filters the population before insights (`eligible = primary[_eligible_mask(primary)]` :435, `filled = ins._filled(eligible)` :436 + hard assert). Pre-fix it loaded raw trades.csv with NO eligibility filter and timeout/window_end force-closed rows fed every aggregate insight (D3).

| insight | src | population | status | note |
|---|---|---|---|---|
| win_rate_pct | insights.py:43-64 | resolved only (BE excluded num+den), None when none resolved | verified | matches settled convention, line-read |
| bootstrap_ci / sharpe / max_drawdown_r / longest_losing_streak | insights.py:71-110 | caller's | verified | standard formulas, seeded rng(42); CI None under n=5 |
| capture_pct | insights.py:113-121 | wins only | verified | avg booked / avg MFE — peak-adjacent by design, guarded by is_peak_metric law |
| compute_overall | insights.py:128-156 | _filled(eligible) | **fixed** | T2 landed: eligibility mask + hard assert; guard tests/test_aggregate_eligibility.py PASS. Area D (2026-07-10): _eligible_mask PROVEN on real data — 10,434 force-closed rows in the canonical file, 0 survive the mask feeding _filled(eligible). Guard ::test_area_d_masks_drop_force_closed_rows_on_canonical_data (bite-proven: mask disabled → force-closed leak into the P&L population). Applies to every _filled(eligible) insight below |
| pair_session_matrix | insights.py:163-194 | _filled(eligible) | **fixed** | T2; per-run tab was already clean; uses UTC-fixed `session` (parked caveat) |
| instrument_verdicts | insights.py:216-255 | _filled(eligible) | **fixed** | T2. PREMIUM_PAIRS still lists NAS100 (dropped pair — harmless, stale) |
| confluence_attribution | insights.py:286-323 | _filled(caller) | verified (population) | 2026-07-10 un-voided: fvg_pts/freshness_pts real & varied in 07-09 baseline (Fix 3 landed pre-run); D3 closed by T2. Population clean. SWEEP LEG STILL out-of-scope (sweep-rebuild) — un-voided only the fvg/freshness/structure legs. Aggregation math re-audited Batch 3: real n/uplift per confluence, coherent verdicts |
| score_validation | insights.py:330-378 | _filled(caller) | verified (population) | 2026-07-10 un-voided: score (4-9, varied) built on alert-time view (T1) with fvg/freshness un-corrupted (Fix 3); D1 structure taint fixed by T1; D3 by T2. Population clean; score validation math re-audited Batch 3: buckets/spearman on varied score, verdict=BROKEN (no edge, real) |
| setup_badge_validation | insights.py:387-454 | _filled(caller) | verified (population) | 2026-07-10 un-voided: setup_badge/_kind populated (2,823 badges, 3 kinds) via alert-time ob_view (T1); D3 by T2. Sweep input stays sweep-workstream-owned. Badge validation math re-audited Batch 3 on populated setup_badge |
| group_comparison | insights.py:456-476 | _filled(eligible) | **fixed** | T2 |
| entry_zone_comparison | insights.py:483-509 | eligible + never_filled (fill-rate denominator) | **fixed** | T2; single proximal bucket now (degenerate, harmless) |
| ob_freshness_comparison | insights.py:533-559 | _filled, keyed on ob_touches | **fixed** | 2026-07-07: re-keyed from the DEAD alert_seq (pinned to 1 by the first-fire dedup, run_backtest.py:161-169) onto ob_touches — the alert-time proximal touch count (touches_at_alert, real 0/1/2 variance). Buckets are now 0/1/2 prior touches and populate for real. No re-run needed: ob_touches already in trades.csv. Guard: tests/test_ob_freshness_column.py (fails if it ever re-keys on a constant column) |
| regime_verification | insights.py:566-605 | _filled(eligible) per run_id | **fixed** | T2 |
| generate_verdict | insights.py:612-679 | upstream dicts | **fixed** (population) | T2; 2026-07-10: its score-issue line no longer inherits a voided score_v — score_validation un-voided against the 07-09 baseline |
| verify_capturable / is_peak_metric | insights.py:723-766 / :715-720 | gate | verified | 2026-07-02 truth-chain audit (peak-vs-fill gate); law block at :682-708. Area D (2026-07-10): gate re-asserted to FIRE — is_peak_metric flags mfe/touch cols, verify_capturable REJECTS a 47%-touched/23%-capturable claim. Guard tests/test_aggregate_eligibility.py::test_area_d_peak_metric_gate_rejects_a_touch_claim (bite-proven: forcing verified=True lets the touch claim through) |
| G1-G10 gates | backtest/scanlog/gates.py (evaluate) + g10_violations :105 | headline | verified | 2026-07-02 truth-chain audit; G10 rule set extracted to pure g10_violations() predicate 2026-07-03 (gate + test bind to one threshold; be_eps FP fix, rule b at +1.001R) |
| headline exclusions (_headline_exclusion) | h1_only_reporting.py:142-176 | audit-only rows out of P&L | verified | 2026-07-02 truth-chain audit + G1 blocked-rows fix |
| killzone alignment table | h1_only_reporting.py:546 (_killzone_alignment_table) | prox_trades (verified population) | verified | aggregates verified killzone_alignment column over headline population |
| driver buckets / two-way | h1_only_reporting.py:814 (_driver_buckets), :869 (_driver_two_way) | prox_trades | verified (population) | 2026-07-10 un-voided: fvg/freshness/badge/bos_verdict dims all populated & varied in 07-09 baseline (Fix 3 + T1 pre-run). Section math re-audited Batch 3. SWEEP dim (if present) stays out-of-scope |
| counterfactual exits | h1_only_reporting.py:627 (_counterfactual_dataframe) | prox_trades | verified | population verified; exit replay = truth-chain audit scope |
| Act 1-6 sections | h1_only_reporting.py (report body) | prox_trades (verified) | verified (population) | aggregation of audited columns over headline population; 2026-07-10: the score/badge/fvg slices are un-voided against the 07-09 baseline (their source columns are clean). Detection-sourced slices (break_*) remain values-stale until the fresh run |
| recipe ranking | h1_only_reporting.py (recipe table) | exit-lab sink (block-stamped, run_backtest.py) | verified (population) | sink rows carry ist/weekend flags so the table drops what the headline drops (826-vs-668 RCA fix) |
| zone register | h1_only_reporting.py:1128 (_build_zone_register_df) | trades_all | verified (population) | |
| Excel tabs (confluences/badges/pair-session/break-ladder) | h1_only_reporting.py:1411 (reference tabs §10), :1475 (_break_ladder_tab_df) | trades_all, written at :3480 (_try_excel(trades_all)); each tab filters to _is_real_filled internally | verified (population) | 2026-07-10 un-voided: confluence/badge/break-ladder tabs' source columns all populated in 07-09 baseline. NOTE: break-ladder reads break_* which are DETECTION-sourced — the break gates were removed 07-10, so break-ladder VALUES are stale until the fresh run (population/plumbing clean, values pending Batch 1). Tab construction re-audited Batch 3 |

## Rules

- No new trades.csv column or emitted insight ships without: (1) a row here, (2) a guard (test/gate) that kills its bug CLASS. (CLAUDE.md rule: ADDED 2026-07-02, Logging section.)
- Insights on pending-fix columns are automatically VOID until the first post-fix baseline run.
- Sweep detector columns/insights: owned by the sweep-rebuild chat. Not audited here.
